# Twitter API (twitterapi.io)
TWITTER_API_KEY=your_api_key
TWITTER_BOT_USERNAME=findthisthread

# Webhook Setup (twitterapi.io)
# Step 1: Create filter rule via API:
#   curl -X POST https://api.twitterapi.io/oapi/tweet_filter/add_rule \
#     -H "Content-Type: application/json" -H "X-API-Key: YOUR_API_KEY" \
#     -d '{"tag":"mentions","value":"@findthisthread","interval_seconds":100}'
#
# Step 2: Activate the rule (use rule_id from step 1):
#   curl -X POST https://api.twitterapi.io/oapi/tweet_filter/update_rule \
#     -H "Content-Type: application/json" -H "X-API-Key: YOUR_API_KEY" \
#     -d '{"rule_id":"RULE_ID","tag":"mentions","value":"@findthisthread","interval_seconds":100,"is_effect":1}'
#
# Step 3: Set webhook URL in twitterapi.io dashboard:
#   - Go to Tweet Filter Rules -> Edit your rule
#   - Set Webhook URL: https://your-app.up.railway.app/webhook/twitter
#
# The webhook verifies requests using X-API-Key header (must match TWITTER_API_KEY)

# Twitter Login (for posting - run: bun run scripts/twitter-login.ts)
TWITTER_PROXY=http://user:pass@ip:port
TWITTER_LOGIN_COOKIE=
# Optional: pre-fill login credentials
# TWITTER_USERNAME=
# TWITTER_EMAIL=
# TWITTER_PASSWORD=
# TWITTER_2FA_SECRET=

# Google Gemini API
GEMINI_API_KEY=your_gemini_key

# Reddit OAuth (optional - for higher rate limits: 60 req/min vs 10)
# Create app at https://www.reddit.com/prefs/apps (type: script)
REDDIT_CLIENT_ID=
REDDIT_CLIENT_SECRET=

# Database
DATABASE_PATH=./data/mentions.db

# Bot Config
POLL_INTERVAL_MS=30000
MAX_IMAGE_SIZE_MB=10
